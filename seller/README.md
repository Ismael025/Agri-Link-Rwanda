# Agri-Link Seller Portal — Independent Assets

Every seller page has its own HTML, CSS and JavaScript file. The Farmer Dashboard uses local SVG icons from the `icons` folder and has no online icon-library dependency.

Open `pages/farmer-dashboard.html` with Live Server in Visual Studio Code.
